<Window x:Class="Quiz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz Game"
        Height="600" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <!--Icon="pack://application:,,,/Resources/quiz_icon.ico">-->

    <Border Background="#1E1E2F" CornerRadius="20" BorderBrush="#444" BorderThickness="1" SnapsToDevicePixels="True">
        <Grid>
            <Rectangle Fill="#99000000"/>

            <DockPanel Height="40" LastChildFill="False" Background="#222" VerticalAlignment="Top"
                       MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                <TextBlock Text="Quiz Game" Foreground="White" VerticalAlignment="Center" Margin="10,0" FontSize="16" />

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="—" Click="Minimize_Click" Style="{StaticResource WindowButtonStyle}" ToolTip="Свернуть" />
                    <Button Content="❐" Click="Maximize_Click" Style="{StaticResource WindowButtonStyle}" ToolTip="Развернуть" />
                    <Button Content="✕" Click="Close_Click" Style="{StaticResource WindowButtonStyle}" ToolTip="Закрыть" />
                </StackPanel>
            </DockPanel>

            <Border Background="#AA1E1E2F" CornerRadius="15" Padding="20" Margin="40,80,40,40">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
                    <TextBlock Text="Выберите категорию викторины" 
                               FontSize="28" FontWeight="Bold" Foreground="White" 
                               TextAlignment="Center" Margin="0 0 0 30"/>

                    <Button Content="Общее" Click="CategoryButton_Click" Style="{StaticResource QuizButtonStyle}" />
                    <Button Content="IT" Click="CategoryButton_Click" Style="{StaticResource QuizButtonStyle}" />
                    <Button Content="Математика" Click="CategoryButton_Click" Style="{StaticResource QuizButtonStyle}" />
                </StackPanel>
            </Border>

        </Grid>
    </Border>
</Window>
